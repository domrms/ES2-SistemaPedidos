name: Auto PR - Develop to Homolog

on:
  push:
    branches:
      - develop

jobs:
  create-pr:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create Pull Request to Homolog
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          PR_EXISTS=$(gh pr list --base homolog --head develop --state open --json number --jq 'length')
          
          if [ "$PR_EXISTS" -eq 0 ]; then
            gh pr create \
              --base homolog \
              --head develop \
              --title "Release: Develop -> Homolog" \
              --body "PR automático para atualização do ambiente de homologação."
            echo "PR criado com sucesso."
          else
            echo "PR de develop para homolog já está aberto. Atualizado com os novos commits."
          fi
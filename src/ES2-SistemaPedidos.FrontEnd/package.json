{
  "name": "es2-sistemapedidos-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "vite --host 0.0.0.0 --port 8000",
    "dev": "npm run start",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.9",
    "jsdom": "^25.0.1",
    "typescript": "^5.7.2",
    "vite": "^7.2.7",
    "vitest": "^4.1.9"
  }
}
